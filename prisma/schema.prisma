generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                Int               @id @default(autoincrement())
  email             String            @unique @db.VarChar(255)
  password          String            @db.VarChar(255)
  name              String            @db.VarChar(255)
  phone             String?           @db.VarChar(50)
  role              UserRole          @default(ADMIN)
  is_active         Boolean           @default(true) @map("is_active")
  created_at        DateTime          @default(now()) @map("created_at") @db.Timestamp(6)
  updated_at        DateTime          @updatedAt @map("updated_at") @db.Timestamp(6)
  expenses          Expense[]
  payments          Payment[]
  productions       Production[]
  purchase_invoices PurchaseInvoice[]
  stock_adjustments StockAdjustment[]
  sale_invoices     SaleInvoice[]
  receipts          Receipt[]
  @@index([email])
  @@map("users")
}

model Supplier {
  id                Int               @id @default(autoincrement())
  name              String
  company_name      String?           @map("company_name")
  phone             String?
  address           String?
  opening_balance   Decimal           @default(0) @map("opening_balance") @db.Decimal(15, 2)
  current_balance   Decimal           @default(0) @map("current_balance") @db.Decimal(15, 2)
  created_at        DateTime          @default(now()) @map("created_at") @db.Timestamp(6)
  updated_at        DateTime          @updatedAt @map("updated_at") @db.Timestamp(6)
  ledger_entries    LedgerEntry[]
  payments          Payment[]
  purchase_invoices PurchaseInvoice[]

  @@map("suppliers")
}

model Customer {
  id              Int           @id @default(autoincrement())
  name            String
  company_name    String?       @map("company_name")
  phone           String?
  address         String?
  opening_balance Decimal       @default(0) @map("opening_balance") @db.Decimal(15, 2)
  current_balance Decimal       @default(0) @map("current_balance") @db.Decimal(15, 2)
  created_at      DateTime      @default(now()) @map("created_at") @db.Timestamp(6)
  updated_at      DateTime      @updatedAt @db.Timestamp(6)
  ledger_entries  LedgerEntry[]
  receipts       Receipt[]  
  sale_invoices SaleInvoice[] 
  @@map("customers")
}

model Category {
  id         Int      @id @default(autoincrement())
  name       String   @unique @db.VarChar(100)
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
  items      Item[]

  @@map("categories")
}

model Item {
  id                     Int                    @id @default(autoincrement())
  name                   String                 @db.VarChar(255)
  item_type              ItemType
  category_id            Int
  quantity               Decimal                @default(0) @db.Decimal(15, 3)
  avg_price              Decimal                @default(0) @db.Decimal(15, 2)
  created_at             DateTime               @default(now())
  updated_at             DateTime               @updatedAt
  unit_type              UnitType               @default(PCS)
  category               Category               @relation(fields: [category_id], references: [id])
  production_ingredients ProductionIngredient[] @relation("ProductionIngredientItem")
  production_items       ProductionItem[]       @relation("ProductionFinalItem")
  purchase_invoice_items PurchaseInvoiceItem[]
  recipe_ingredients     RecipeIngredient[]     @relation("RecipeIngredientItem")
  recipe                 Recipe?                @relation("RecipeFinalProduct")
  stock_adjustments      StockAdjustment[]
  sale_invoice_items     SaleInvoiceItem[] 

  @@unique([category_id, name])
  @@index([category_id])
  @@index([item_type])
  @@map("items")
}

model StockAdjustment {
  id                  Int              @id @default(autoincrement())
  item_id             Int
  admin_id            Int
  quantity            Decimal          @db.Decimal(15, 3)
  avg_price           Decimal          @db.Decimal(15, 2)
  reason              String
  notes               String?
  adjustment_date     DateTime         @default(now())
  created_at          DateTime         @default(now())
  purchase_invoice_id Int?
  production_id       Int?
  admin               User             @relation(fields: [admin_id], references: [id])
  item                Item             @relation(fields: [item_id], references: [id])
  production          Production?      @relation(fields: [production_id], references: [id])
  purchase_invoice    PurchaseInvoice? @relation(fields: [purchase_invoice_id], references: [id])

  @@map("stock_adjustments")
}

model Account {
  id              Int           @id @default(autoincrement())
  name            String        @db.VarChar(100)
  account_type    AccountType
  account_number  String?       @db.VarChar(50)
  bank_name       String?       @db.VarChar(100)
  opening_balance Decimal       @default(0) @db.Decimal(15, 2)
  current_balance Decimal       @default(0) @db.Decimal(15, 2)
  created_at      DateTime      @default(now()) @db.Timestamp(6)
  updated_at      DateTime      @updatedAt @db.Timestamp(6)
  expenses        Expense[]
  ledger_entries  LedgerEntry[]
  payments        Payment[]
  receipts       Receipt[]  

  @@map("accounts")
}

model PurchaseInvoice {
  id                Int                   @id @default(autoincrement())
  invoice_number    String                @unique @db.VarChar(50)
  supplier_id       Int
  admin_id          Int
  invoice_date      DateTime              @default(now()) @db.Timestamp(6)
  due_date          DateTime?             @db.Timestamp(6)
  subtotal          Decimal               @db.Decimal(15, 2)
  tax               Decimal               @default(0) @db.Decimal(15, 2)
  discount          Decimal               @default(0) @db.Decimal(15, 2)
  total_amount      Decimal               @db.Decimal(15, 2)
  paid_amount       Decimal               @default(0) @db.Decimal(15, 2)
  payment_status    PaymentStatus         @default(UNPAID)
  notes             String?
  created_at        DateTime              @default(now()) @db.Timestamp(6)
  updated_at        DateTime              @updatedAt @db.Timestamp(6)
  ledger_entries    LedgerEntry[]
  payments          Payment[]
  items             PurchaseInvoiceItem[]
  admin             User                  @relation(fields: [admin_id], references: [id])
  supplier          Supplier              @relation(fields: [supplier_id], references: [id])
  stock_adjustments StockAdjustment[]

  @@index([supplier_id])
  @@index([invoice_date])
  @@index([payment_status])
  @@map("purchase_invoices")
}

model PurchaseInvoiceItem {
  id                  Int             @id @default(autoincrement())
  purchase_invoice_id Int
  item_id             Int
  quantity            Decimal         @db.Decimal(15, 3)
  unit_price          Decimal         @db.Decimal(15, 2)
  total_price         Decimal         @db.Decimal(15, 2)
  created_at          DateTime        @default(now()) @db.Timestamp(6)
  item                Item            @relation(fields: [item_id], references: [id])
  purchase_invoice    PurchaseInvoice @relation(fields: [purchase_invoice_id], references: [id], onDelete: Cascade)

  @@index([purchase_invoice_id])
  @@index([item_id])
  @@map("purchase_invoice_items")
}
model SaleInvoice {
  id              Int           @id @default(autoincrement())
  invoice_number  String        @unique @db.VarChar(50)    // ✅ ADDED db.VarChar
  customer_id     Int
  customer        Customer      @relation(fields: [customer_id], references: [id], onDelete: Restrict)
  admin_id        Int
  admin           User          @relation(fields: [admin_id], references: [id], onDelete: Restrict)
  invoice_date    DateTime      @default(now()) @db.Timestamp(6)  // ✅ ADDED db.Timestamp
  due_date        DateTime?     @db.Timestamp(6)                  // ✅ ADDED db.Timestamp
  subtotal        Decimal       @db.Decimal(15, 2)
  tax             Decimal       @default(0) @db.Decimal(15, 2)
  discount        Decimal       @default(0) @db.Decimal(15, 2)
  total_amount    Decimal       @db.Decimal(15, 2)
  received_amount Decimal       @default(0) @db.Decimal(15, 2)
  payment_status  PaymentStatus @default(UNPAID)
  notes           String?
  created_at      DateTime      @default(now()) @db.Timestamp(6)  // ✅ ADDED db.Timestamp
  updated_at      DateTime      @updatedAt @db.Timestamp(6)       // ✅ ADDED db.Timestamp

  items          SaleInvoiceItem[]
  receipts       Receipt[]
  ledger_entries LedgerEntry[]

  @@index([customer_id])      // ✅ ADDED
  @@index([invoice_date])     // ✅ ADDED
  @@index([payment_status])   // ✅ ADDED
  @@map("sale_invoices")
}
model SaleInvoiceItem {
  id                 Int             @id @default(autoincrement())
  sale_invoice_id    Int
  sale_invoice       SaleInvoice     @relation(fields: [sale_invoice_id], references: [id], onDelete: Cascade)
  item_id            Int
  item               Item            @relation(fields: [item_id], references: [id], onDelete: Restrict)
  production_item_id Int?
  production_item    ProductionItem? @relation(fields: [production_item_id], references: [id], onDelete: SetNull)
  
  // Snapshot fields
  item_name          String          @db.VarChar(255)    // ✅ ADDED
  serial_number      String?         @db.VarChar(100)    // ✅ ADDED
  
  // Pricing fields
  quantity           Decimal         @db.Decimal(15, 3)
  unit_price         Decimal         @db.Decimal(15, 2)
  cost_price         Decimal         @db.Decimal(15, 2)  // ✅ ADDED
  total_price        Decimal         @db.Decimal(15, 2)
  profit             Decimal         @db.Decimal(15, 2)  // ✅ ADDED
  
  created_at         DateTime        @default(now()) @db.Timestamp(6)  // ✅ ADDED db.Timestamp

  @@index([sale_invoice_id])
  @@index([item_id])
  @@index([production_item_id])   // ✅ ADDED
  @@map("sale_invoice_items")
}

model Payment {
  id                  Int              @id @default(autoincrement())
  payment_number      String           @unique @db.VarChar(50)
  supplier_id         Int
  account_id          Int
  admin_id            Int
  amount              Decimal          @db.Decimal(15, 2)
  payment_date        DateTime         @default(now()) @db.Timestamp(6)
  purchase_invoice_id Int?
  notes               String?
  created_at          DateTime         @default(now()) @db.Timestamp(6)
  updated_at          DateTime         @updatedAt @db.Timestamp(6)
  ledger_entries      LedgerEntry[]
  account             Account          @relation(fields: [account_id], references: [id])
  admin               User             @relation(fields: [admin_id], references: [id])
  purchase_invoice    PurchaseInvoice? @relation(fields: [purchase_invoice_id], references: [id])
  supplier            Supplier         @relation(fields: [supplier_id], references: [id])

  @@index([supplier_id])
  @@index([account_id])
  @@index([payment_date])
  @@map("payments")
}
model Receipt {
  id              Int          @id @default(autoincrement())
  receipt_number  String       @unique @db.VarChar(50)     // ✅ ADDED db.VarChar
  customer_id     Int
  customer        Customer     @relation(fields: [customer_id], references: [id], onDelete: Restrict)
  account_id      Int
  account         Account      @relation(fields: [account_id], references: [id], onDelete: Restrict)
  admin_id        Int
  admin           User         @relation(fields: [admin_id], references: [id], onDelete: Restrict)
  amount          Decimal      @db.Decimal(15, 2)
  receipt_date    DateTime     @default(now()) @db.Timestamp(6)  // ✅ ADDED db.Timestamp
  sale_invoice_id Int?
  sale_invoice    SaleInvoice? @relation(fields: [sale_invoice_id], references: [id], onDelete: SetNull)
  notes           String?
  created_at      DateTime     @default(now()) @db.Timestamp(6)  // ✅ ADDED db.Timestamp
  updated_at      DateTime     @updatedAt @db.Timestamp(6)       // ✅ ADDED db.Timestamp

  ledger_entries LedgerEntry[]

  @@index([customer_id])      // ✅ ADDED
  @@index([account_id])       // ✅ ADDED
  @@index([receipt_date])     // ✅ ADDED
  @@map("receipts")
}

model LedgerEntry {
  id                  Int              @id @default(autoincrement())
  entity_type         LedgerEntityType
  supplier_id         Int?
  customer_id         Int?
  account_id          Int?
  transaction_type    TransactionType
  amount              Decimal          @db.Decimal(15, 2)
  balance             Decimal          @db.Decimal(15, 2)
  description         String
  reference_number    String?          @db.VarChar(50)
  transaction_date    DateTime         @default(now()) @db.Timestamp(6)
  created_at          DateTime         @default(now()) @db.Timestamp(6)
  purchase_invoice_id Int?
  payment_id          Int?
  receipt_id          Int?              // ✅ ADDED
  expense_id          Int?
  sale_invoice_id     Int?
  sale_invoice        SaleInvoice?     @relation(fields: [sale_invoice_id], references: [id], onDelete: SetNull)
  receipt             Receipt?          @relation(fields: [receipt_id], references: [id], onDelete: SetNull)  // ✅ ADDED
  account             Account?         @relation(fields: [account_id], references: [id], onDelete: Cascade)
  customer            Customer?        @relation(fields: [customer_id], references: [id], onDelete: Cascade)
  expense             Expense?         @relation(fields: [expense_id], references: [id])
  payment             Payment?         @relation(fields: [payment_id], references: [id])
  purchase_invoice    PurchaseInvoice? @relation(fields: [purchase_invoice_id], references: [id])
  supplier            Supplier?        @relation(fields: [supplier_id], references: [id], onDelete: Cascade)

  @@index([supplier_id])
  @@index([customer_id])
  @@index([account_id])
  @@index([transaction_date])
  @@index([receipt_id])   
  @@map("ledger_entries")
}

model Recipe {
  id               Int                @id @default(autoincrement())
  final_product_id Int                @unique
  name             String             @db.VarChar(255)
  description      String?
  created_at       DateTime           @default(now()) @db.Timestamp(6)
  updated_at       DateTime           @updatedAt @db.Timestamp(6)
  productions      Production[]
  ingredients      RecipeIngredient[]
  final_product    Item               @relation("RecipeFinalProduct", fields: [final_product_id], references: [id])

  @@map("recipes")
}

model RecipeIngredient {
  id         Int      @id @default(autoincrement())
  recipe_id  Int
  item_id    Int
  quantity   Decimal  @db.Decimal(15, 2)
  created_at DateTime @default(now()) @db.Timestamp(6)
  updated_at DateTime @updatedAt @db.Timestamp(6)
  item       Item     @relation("RecipeIngredientItem", fields: [item_id], references: [id])
  recipe     Recipe   @relation(fields: [recipe_id], references: [id], onDelete: Cascade)

  @@unique([recipe_id, item_id])
  @@index([recipe_id])
  @@index([item_id])
  @@map("recipe_ingredients")
}

model Production {
  id                     Int                    @id @default(autoincrement())
  batch_number           String                 @unique @db.VarChar(50)
  recipe_id              Int
  admin_id               Int
  quantity               Int
  status                 ProductionStatus       @default(DRAFT)
  total_cost             Decimal                @default(0) @db.Decimal(15, 2)
  cost_per_unit          Decimal                @default(0) @db.Decimal(15, 2)
  start_date             DateTime?              @db.Timestamp(6)
  completion_date        DateTime?              @db.Timestamp(6)
  notes                  String?
  created_at             DateTime               @default(now()) @db.Timestamp(6)
  updated_at             DateTime               @updatedAt @db.Timestamp(6)
  production_ingredients ProductionIngredient[]
  production_items       ProductionItem[]
  admin                  User                   @relation(fields: [admin_id], references: [id])
  recipe                 Recipe                 @relation(fields: [recipe_id], references: [id])
  stock_adjustments      StockAdjustment[]

  @@index([recipe_id])
  @@index([status])
  @@map("productions")
}

model ProductionIngredient {
  id             Int        @id @default(autoincrement())
  production_id  Int
  item_id        Int
  quantity       Decimal    @db.Decimal(15, 2)
  is_from_recipe Boolean    @default(true)
  created_at     DateTime   @default(now()) @db.Timestamp(6)
  updated_at     DateTime   @updatedAt @db.Timestamp(6)
  item           Item       @relation("ProductionIngredientItem", fields: [item_id], references: [id])
  production     Production @relation(fields: [production_id], references: [id], onDelete: Cascade)

  @@unique([production_id, item_id])
  @@index([item_id])
  @@index([production_id])
  @@map("production_ingredients")
}

model ProductionItem {
  id            Int        @id @default(autoincrement())
  production_id Int
  item_id       Int
  serial_number String     @unique @db.VarChar(100)
  cost_price    Decimal    @db.Decimal(15, 2)
  is_sold       Boolean    @default(false)
  created_at    DateTime   @default(now()) @db.Timestamp(6)
  updated_at    DateTime   @updatedAt @db.Timestamp(6)
  item          Item       @relation("ProductionFinalItem", fields: [item_id], references: [id])
  production    Production @relation(fields: [production_id], references: [id], onDelete: Cascade)
  sale_invoice_items SaleInvoiceItem[]
  @@index([production_id])
  @@index([item_id])
  @@index([serial_number])
  @@index([is_sold])
  @@map("production_items")
}

model ExpenseCategory {
  id          Int       @id @default(autoincrement())
  name        String    @unique
  description String?
  created_at  DateTime  @default(now())
  updated_at  DateTime  @updatedAt
  expenses    Expense[]

  @@map("expense_categories")
}

model Expense {
  id             Int             @id @default(autoincrement())
  category_id    Int
  account_id     Int
  admin_id       Int
  amount         Decimal         @db.Decimal(15, 2)
  description    String
  expense_date   DateTime        @default(now())
  notes          String?
  receipt_image  String?
  created_at     DateTime        @default(now())
  updated_at     DateTime        @updatedAt
  account        Account         @relation(fields: [account_id], references: [id])
  admin          User            @relation(fields: [admin_id], references: [id])
  category       ExpenseCategory @relation(fields: [category_id], references: [id])
  ledger_entries LedgerEntry[]

  @@map("expenses")
}

enum UserRole {
  ADMIN
}

enum ItemType {
  RAW
  FINAL
}

enum UnitType {
  PCS
  SET
}

enum AccountType {
  BANK
  JAZZCASH
  EASYPAISA
  IN_HAND
}

enum PaymentStatus {
  PAID
  UNPAID
  PARTIAL
}

enum LedgerEntityType {
  SUPPLIER
  CUSTOMER
  ACCOUNT
}

enum TransactionType {
  CREDIT
  DEBIT
}

enum ProductionStatus {
  DRAFT
  IN_PROCESS
  DONE
}
