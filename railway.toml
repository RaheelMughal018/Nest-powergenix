# Railway deployment config
# https://docs.railway.com/config-as-code
# Uses Dockerfile when present; start is handled by Dockerfile CMD.

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
