# Railway deployment config
# https://docs.railway.com/config-as-code

[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "prisma migrate deploy && npm run start:prod"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
